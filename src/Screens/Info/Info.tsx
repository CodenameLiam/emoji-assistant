import React, { FC } from 'react';
import { View, Text } from 'react-native';

const Info: FC = () => {
    return (
        <View>
            <Text>Info</Text>
        </View>
    );
};

export default Info;
