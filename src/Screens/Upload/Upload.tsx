import React, { FC } from 'react';
import { View, Text } from 'react-native';

const Upload: FC = () => {
    return (
        <View>
            <Text>Upload</Text>
        </View>
    );
};

export default Upload;
